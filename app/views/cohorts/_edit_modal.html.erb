  <div id="<%= cohort.id %>_edit_modal" class="fixed inset-0 z-50 hidden bg-gray-900 bg-opacity-50 flex items-center justify-center">
    <div class="bg-white w-full max-w-3xl rounded-lg p-4 relative">
      <button type="button" class="absolute top-3 right-3 text-gray-500 hover:text-gray-700"
              onclick="document.getElementById('<%= cohort.id %>_edit_modal').classList.add('hidden')">
        &times;
      </button>
      <%= render partial: "cohorts/form", locals: { cohort: cohort, current_program: @program } %>
    </div>
  </div>